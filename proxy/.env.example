# 프록시가 어디에 포트를 열지.
#
#   로컬(윈도우)  : 80:80
#   사내 서버     : 127.0.0.1:8081:80
#                   앞에 호스트 nginx 가 있으므로 80 을 가져가면 안 되고,
#                   127.0.0.1 을 붙여 바깥에서 직접 못 들어오게 한다.
PROXY_PUBLISH=80:80
