fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
python-dotenv>=1.0
PyJWT>=2.8

# 챗 파일 첨부용. FastAPI 가 multipart/form-data 를 받으려면 반드시 필요하다.
# 없으면 앱이 뜨다가 죽는다 —
#   RuntimeError: Form data requires "python-multipart" to be installed.
# 컨테이너가 안 뜨니 프록시에는 502 로만 보이고, 진짜 이유는 로그에만 있다.
python-multipart>=0.0.9

# 사용하는 공급자 것만 설치해도 된다 (지연 import).
# 비즈라우터처럼 OpenAI 호환 게이트웨이만 쓴다면 anthropic 은 지워도 된다.
openai>=1.40
anthropic>=0.40

# 없으면 ZoneInfo("Asia/Seoul") 에서 오류가 난다.
# 리눅스 컨테이너에는 대개 있지만, 윈도우에서 직접 돌릴 때 반드시 필요하다.
tzdata>=2024.1

# ── 첨부 파일에서 글자 뽑기 (app/filetext.py) ──────────────
# 전용 앱이 없는 파일을 "이거 요약해줘" 로 처리할 때 쓴다.
# 셋 다 순수 파이썬이라 설치가 가볍고 빌드 도구가 필요 없다.
pypdf>=4.2
python-docx>=1.1
openpyxl>=3.1
python-pptx>=1.0

# pandas 는 쓰지 않는다. 엑셀 집계는 ai-report 가 하고,
# 포털은 그 API 에 물어보기만 한다. (app/tools/ai_report.py 참고)
# openpyxl 은 집계용이 아니라 첨부 파일을 읽기 위한 것이다.
