# 포털이 서버 대 서버로 이 앱을 부를 때 쓰는 공유 토큰.
# ★ portal/.env 의 AUDIT_TOKEN 과 **글자 하나까지 같아야 한다.**
#   다르면 포털이 이 앱을 못 부르고 "Unauthorized" 만 돌아온다.
#   ../setup.sh (또는 setup.ps1) 이 세 파일에 같은 값을 맞춰 넣어 준다.
AUDIT_TOKEN=
