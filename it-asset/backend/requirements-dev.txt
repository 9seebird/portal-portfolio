# 테스트 실행에만 필요한 도구. 운영 서버에는 설치하지 않는다.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# httpx2 는 starlette 1.3 의 TestClient 가 쓰는 HTTP 클라이언트다.
# 이게 없으면 pytest 실행 시 다음 오류가 난다:
#   RuntimeError: The starlette.testclient module requires the httpx2 package
-r requirements.txt
pytest==8.3.4
httpx2==2.9.1
